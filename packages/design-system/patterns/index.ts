/**
 * Shared layout patterns — to be populated as patterns are extracted.
 *
 * Planned patterns:
 *   - SidebarLayout (fixed sidebar + scrollable main)
 *   - WorkspaceShell (full-height flex with header/main/footer)
 *   - FullScreenOverlay (z-indexed full-viewport layer)
 *
 * For now, each repo implements these as local layout components.
 */

export {}
